# Tess AI 智能归因与诊断系统 PRD V1.0.0 评审意见

> 评审对象：用户提供的《Teensing 平台：Tess AI 智能归因与诊断系统 PRD》V1.0.0（草案/待评审）。
> 评审基准：上一轮对 1.1–1.2 片段的评审（P0×5、P1×4）。
> 总体判断：V1.0.0 已从「愿景宣言」升级为「可落地的功能+接口草案」，上一轮 9 个缺口中 **5 个已闭合、3 个部分闭合、1 个未动**。但作为一份要交给 dev/UI 直接落地的文档，仍有 3 个 P0 级隐患（置信度/兜底、Severity 与金额量化的确定性）必须补，否则诊断结论不可信、不可验收。

---

## 一、上一轮缺口的闭合情况

| 上轮缺口 | 状态 | 本版对应与说明 |
|---|---|---|
| P0-1 补「感知→定位→归因→建议→处置」哪一环 | ✅ 已闭合 | §2.1 明确链路：触发→特征提取→AI 推理→UI 闭环；§3.2 输出结论+因果链+建议+跳转 |
| P0-2 与一/二阶段关系零定义 | 🟡 部分闭合 | §2.1.1 触发层含「异常池点击 / 定时扫描」，定位为异常池下游；但缺一句「Tess = 异常池之上的归因层」的明确定位，且 §4.1 输入 schema 未与异常池字段做映射 |
| P0-3 运营/运维 persona 混用 | ❌ 未闭合 | 全文仍混用，无两类角色拆分；UI 角色无关尚可接受，但 1.1 痛点应点明对谁痛 |
| P0-4 归因可信度/置信度/错误兜底 | 🟡 部分闭合 | §5「严禁凭空编造」+§6.3 脱敏 ✅；但 ❌ 无 confidence 字段、❌ 无「数据不足无法确定根因」分支、❌ 无错误归因兜底 |
| P0-5 「可执行」程度未界定 | ✅ 已闭合（更务实） | §3.2.D 改为「一键跳转路由」而非自动执行，首版定位合理 |
| P1-6 LLM 访问日志/配置安全边界 | ✅ 已闭合 | §6.3 明确抹去 API Key/密钥，仅透出 ID+统计值 |
| P1-7 LLM 与归因算法分工 | ✅ 已闭合 | §2.1.2/2.1.3 明确：算法层算变动/贡献度，AI 层做推理生成 |
| P1-8 量化基线/北极星指标 | ❌ 未闭合 | 仅 §6.1「响应<3s / temperature 0.1–0.2」；无业务指标（排查耗时降幅、归因准确率、覆盖率、误/漏报） |
| P1-9 触发方式/输入粒度 | 🟡 部分闭合 | §2.1.1 定义触发，§4.1 为事件级输入；但三入口是否都喂同一 schema 未明示 |

---

## 二、本版新增的关键问题（按严重度）

### [P0] N1. 诊断系统缺「置信度」与「无法确定」分支 —— 最危险隐患
一个会" Always assert a root cause "的诊断工具，在数据稀薄时必然**自信地幻觉**。当前：
- §4.2 输出无 `confidence` 字段（仅 severity，且 severity 不等同置信度）；
- §5 Prompt 隐含假设"一定能推导出根因"，未授权模型在证据不足时返回 `INCONCLUSIVE`；
- 无"错误归因如何纠偏/转人工"的兜底路径。
- **修订**：① §4.2 增加 `confidence: high|medium|low` 与可选 `uncertainty_note`；② 允许 `root_cause_analysis` 为 null 且 `status: "INCONCLUSIVE"`，并在 Prompt 中明确要求"证据不足时必须返回 INCONCLUSIVE 并列出所需补充数据"；③ 前端对 low/inconclusive 展示「建议人工复核」CTA。

### [P0] N2. Severity 不应交由 LLM 判定 —— 破坏 §6.1 的「确定性」目标
§4.2 有 `severity`，§3.2.A 有红/橙/黄/蓝标签，但**谁定 severity 完全没说**。若让 LLM 自由定，则与 §6.1「temperature 0.1–0.2 保证确定性」自相矛盾，且同事件两次诊断可能不同级。
- **修订**：severity 由**算法层按业务阈值规则**计算（如毛利率降幅≥X%→CRITICAL），作为 §4.1 字段输入，LLM 只展示不决策。

### [P0] N3. 金额类量化（预估损耗 $350/小时）不应由 LLM 自由生成
§3.2.C 示例"预估损耗 $350/小时"是业务决策依据，但 §4.2 `estimated_loss_risk` 是 LLM 自由文本。LLM 编的金额不可信、不可复现。
- **修订**：损耗由**算法层**按 `流量 × 毛利缺口 × 单价` 计算成数值，LLM 仅做自然语一言转述，且输出应带数值字段而非纯文本。

### [P1] N4. Prompt 与 §4.2 Schema 未绑定，存在漂移风险
§5 说"返回符合指定 JSON Schema 的 JSON"，但 Prompt 正文并未嵌入/引用 §4.2 的字段名与类型。两份定义分离，后续任一处改动都会让另一处失准。
- **修订**：将 §4.2 schema（或字段清单+示例）直接注入 System Prompt，或改为 function-calling / structured-output 强制约束，避免"前导文字"类解析失败。

### [P1] N5. impact_scope 自由文本 vs 算法层 top_contributors 可能不一致
§4.1 已提供结构化 `top_contributors`（维度+贡献度），但 §4.2 `impact_scope.affected_dimensions` 是 LLM 自由文本，可能改写/遗漏算法层已算好的贡献度，造成 UI 与数据打架。
- **修订**：`impact_scope` 直接引用 `top_contributors` 的 ID，不做二次自由生成；LLM 只补充自然语言描述。

### [P1] N6. 原始痛点「配置文件比对」未被设计覆盖
1.1 痛点是"在多维数据表、日志系统、**配置文件**之间来回比对"，但 §2.1 算法层只提"提取相关日志"、§4.1 也无配置相关信号。设计实际只解决了「指标+日志」两维，**没解决配置比对**。
- **修订**：要么在 §4.1 `associated_signals` 增加配置类信号源（如"最近 1h 配置变更记录"），要么在文档中明确"配置比对不在 V1 范围"。

### [P1] N7. 算法层失败的兜底缺失
§6.2 只覆盖"LLM 超时/解析失败 → 展示算法层原始 Top N"。但若**算法层本身失败**（取数/计算异常），抽屉会空或报错，无兜底。
- **修订**：增加算法层异常的兜底态（如"特征提取暂不可用，请稍后重试 / 转人工"）。

### [P1] N8. 流式输出 vs 严格 JSON 的矛盾
§6.1「流式输出或 JSON 解析」+ Prompt 禁前导文字。流式 JSON 在中途解析脆弱，且若模型先吐前言再吐 JSON 会解析失败。
- **修订**：明确采用"后端先拿到完整 JSON 再逐字段推给前端"的伪流式，或走 SSE + 严格 `` 边界解析；Prompt 禁前导文字需配合输出模式保证。

### [P2] N9. 反馈闭环缺评估集，无法衡量准确率
§3.2.E 收集 👍/👎 → 记日志"用于 Prompt 优化"，但没有标注 gold-set、没有准确率度量方法。诊断类工具没有评估集等于盲飞。
- **修订**：建立抽样标注集 + 定期归因准确率/有用率复盘，作为 §6 的验收指标之一。

### [P2] N10. 数据窗口"1 小时"硬编码
§3.2.B 文案"交叉比对过去 1 小时"，但 §4.1 信号含 13:30–14:00 等具体窗口。窗口应可配置参数，而非写死在 micro-copy。

### [P2] N11. 缺 Out of Scope / 依赖前提 / DoD（交付 dev/UI 必需）
作为要直接落地的文档，缺三块：① **范围排除**（V1 不做什么，如自动处置、多语言）；② **依赖与前提**（异常池/日志/统计引擎哪些接口须先就绪）；③ **验收标准 DoD**（每条 FR 的 done 定义）。

### [P2] N12. 多入口一致性未约束
§3.1 三个入口（异常池 / 全局预警 / 大盘浮标）。"大盘指标剧烈波动"的"剧烈"阈值未定义；三入口是否都喂同一 §4.1 schema 应明示，否则后端要维护多套输入。

---

## 三、交付级缺口（给 dev/UI 前必补）

| 缺失项 | 为什么必须 | 建议位置 |
|---|---|---|
| Out of Scope（范围排除） | 防止 scope creep，明确 V1 边界 | 新增 §0 或 §7 |
| 依赖与前提（Dependencies） | dev 排期依赖：异常池/日志/统计引擎接口就绪状态 | 新增 §7 |
| 验收标准 DoD | UI/dev 不知"做完"的判据 | 每条 FR 后或独立 § |
| 评估指标（准确率/有用率） | 对应 N9，闭环优化前提 | §6 NFR 增补 |

---

## 四、修订清单（按优先级）

**P0（不补不可交付）**
1. N1 增加 `confidence` 字段 + `INCONCLUSIVE` 分支 + 低置信转人工 CTA。
2. N2 severity 改由算法层按阈值规则计算，作为 §4.1 输入，LLM 只展示。
3. N3 损耗金额改由算法层计算数值，LLM 仅转述；输出带数值字段。

**P1（强烈建议补）**
4. N4 将 §4.2 schema 注入 System Prompt 或用 function-calling 强约束。
5. N5 `impact_scope` 引用 `top_contributors` ID，禁二次自由生成。
6. N6 补配置类信号源，或明示"配置比对不在 V1"。
7. N7 增加算法层失败的兜底态。
8. N8 明确流式/JSON 解析的实现契约。

**P2（文档完整性）**
9. N9 建标注集 + 准确率复盘机制。
10. N10 数据窗口参数化。
11. N11 补 Out of Scope / 依赖 / DoD。
12. N12 统一三入口输入 schema，定义"剧烈波动"阈值。

---

## 给评审会的一句话
> V1.0.0 已具备落地骨架，但一个"诊断系统"若让 LLM 自己定严重程度、自己编损失金额、且无法说"我不确定"，它就不是一个可信的诊断系统。把 N1/N2/N3 三件事定下来，这份文档就能放心交给 dev/UI。
